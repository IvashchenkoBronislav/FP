function GoogleReadyHandlerumvfedfk() {
    var infoWindow, map = new google.maps.Map(document.getElementById("mapumvfedfk"), {
        center: { lat: 50.45466, lng: 30.5238 },
        zoom: 12,
        disableDefaultUI: true,
        zoomControl: true,
        mapTypeControl: true,
        mapTypeId: "roadmap"
    });
    return map;
}